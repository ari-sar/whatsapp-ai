import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'meetwave-client-portal',
  webDir: 'dist'
};

export default config;
